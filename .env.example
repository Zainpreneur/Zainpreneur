# ── Database ──
DATABASE_URL=postgresql://zainpreneur:password@localhost:5432/zainpreneur

# ── Auth ──
JWT_SECRET=change-me-to-a-random-string-at-least-32-characters
JWT_EXPIRES_IN=7d

# ── Server ──
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# ── CORS ──
CORS_ORIGIN=http://localhost:5900

# ── Logging ──
LOG_LEVEL=info
